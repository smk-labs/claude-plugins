# core.autocrlf is true on the Windows machine, but the tools that write
# these files (editors, agents, scripts) write LF. Git then reports a clean
# tree as fully modified, which hides real work and blocks a pull. Pinning
# LF in the working tree makes both sides agree.
* text=auto eol=lf
