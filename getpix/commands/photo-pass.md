---
description: Art-direction photo pass over the site: add photography only where it earns its place
argument-hint: "[optional scope, e.g. only the landing page]"
---

Use the photo-pass skill to run a full art-direction photo pass. Scope: $ARGUMENTS

If no scope was given, cover the whole site.
