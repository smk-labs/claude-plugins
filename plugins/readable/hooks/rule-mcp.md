<!-- Tier-3 dispatch rule. Active ONLY when hooks.json points here instead of rule.md. Requires the readable-card MCP server AND a host that renders MCP Apps (verify first: in a fresh session, ask for a reply "با تول card" and confirm a styled card appears). Rollback: point hooks.json back at rule.md. -->
<readable-rtl-rule>
Persian/RTL replies scramble as plain chat text. In this client, deliver every Persian (or any RTL) reply as ONE call to the `card` tool (readable-card MCP server). Pass ONLY the reply content as HTML in the `html` argument, built from these blocks: <h2> once as title; <p class="lead"> muted intro; <h3> per section; <p>; <ul>/<ol>; <li class="ok">/<li class="no">; callouts <div class="cal tip|note|warn|danger"><div>text</div></div>; <code> around every path/command/URL/code token; <a>; <strong>; <hr>; table <table><thead><tbody> with <span class="badge ok|warn|info"> chips; key-values <div class="kv"><div><b>k</b><span>v</span></div>...</div>; KPI cards <div class="grid c3|c2"> of <div class="kpi"><div class="l">label</div><div class="n">1.2M<span class="trend up">18%</span></div></div>; bars <div class="bars"><div class="bar"><span class="l">l</span><span class="t"><i style="width:72%"></i></span><span class="v">72%</span></div></div>; donut <div class="donut-w"><div class="donut" style="--a:46;--b:31"></div><div class="leg"><span class="a"><i></i>l 46%</span>...</div></div>; flow <div class="flow"><span class="s">step</span>...</div>; timeline <div class="tl"><div><b>t</b>text</div>...</div>; optional CTA <div class="btns"><button class="cta" onclick="sendPrompt('...')">l</button></div>. NO <style>, NO <script>, NO wrapper div: the server template styles everything.

The tool call IS the whole reply; output nothing after it and never repeat the content as plain text. Keep any unavoidable chat text short and in English. Very short replies (1-2 plain sentences, no code): skip the tool, answer as BiDi-safe plain text (start each line with a strong RTL character). If the `card` tool is unavailable or its result says the host did not render it, reply in English (or BiDi-safe plain text) and tell the user in one English line to switch hooks.json back to rule.md.
</readable-rtl-rule>
