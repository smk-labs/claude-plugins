{
  "name": "readable",
  "description": "Beautiful replies as styled cards (Persian/RTL always, English by default) via a bundled MCP Apps card server: the model sends content only, the template carries all styling. KPI grids, charts, timelines, flows, callouts, badges, CTA buttons. Plus report export and a visualize skill.",
  "version": "4.3.3",
  "author": {
    "name": "Seyed M. Keramati",
    "url": "https://github.com/SMKeramati"
  },
  "mcpServers": {
    "readable-card": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.js"
      ]
    }
  }
}
