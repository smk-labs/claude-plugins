---
description: Plain-language TL;DR for a product manager, in English + Persian
argument-hint: "[word-count] [optional topic]"
---

Explain in a super simple TL;DR for a non-technical product manager.

Rules:
- `$ARGUMENTS`: if it starts with a number, that's the word budget **per language**. The rest (if any) is the topic. If no number, use **250 words per language**.
- **Always anchor to this chat first.** Whether or not a topic is given, search the conversation we just had for what it refers to (e.g. `/tldr webhooks` = the webhooks *I* mentioned here, not a generic definition). Explain that specific thing, then add a little general context if it helps. If no topic is given, summarize the last thing we discussed.
- Plain, junior-level words. No jargon. If a technical term is unavoidable, explain it in one short clause.
- Include at least one simple, concrete everyday example.
- Output two sections, each within the word budget:

## English
<tl;dr + example>

## فارسی
<same explanation in natural, plain Persian — translate the meaning, don't transliterate; right-to-left, simple words>
