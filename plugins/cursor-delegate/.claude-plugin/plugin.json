{
  "name": "cursor-delegate",
  "description": "Delegate self-contained coding/research tasks to the Cursor CLI (cursor-agent) from inside Claude Code, so they run on the Cursor subscription's quota instead of Claude's. Claude stays the orchestrator; Cursor runs the slice. Works with your single Cursor login out of the box; an optional account input targets a specific seat when you have several. Agent calling, not a model-backend swap.",
  "version": "1.0.0",
  "author": {
    "name": "Seyed M. Keramati",
    "url": "https://github.com/SMKeramati"
  },
  "mcpServers": {
    "cursor-delegate": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/cursor-mcp.js"],
      "env": {
        "CURSOR_RUN_BIN": "${CLAUDE_PLUGIN_ROOT}/scripts/cursor-run.sh"
      }
    }
  }
}
